export default function GoogleTranslateButton() {
  // El widget se renderiza solo dentro de #gt_placeholder (index.html)
  // Aquí solo colocas el espacio donde lo verás en tu layout.
  return <div id="gt_placeholder" />;
}
